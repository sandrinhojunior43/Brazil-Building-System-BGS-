// Preencha com os valores do SEU projeto Supabase (Project Settings → API).
// SUPABASE_URL e SUPABASE_ANON_KEY são chaves PÚBLICAS — foram feitas para
// rodar no navegador. NUNCA coloque aqui a service_role key (essa fica só
// nas variáveis de ambiente da Vercel, usada pelos arquivos em /api).
window.BGS_CONFIG = {
  SUPABASE_URL: 'https://xxxxx.supabase.co',
  SUPABASE_ANON_KEY: 'sua_anon_key_aqui',
};
